(()=>{const intro=document.querySelector('#intro'),final=document.querySelector('#final'),stage=document.querySelector('#stage'),cat=document.querySelector('#cat'),yarn=document.querySelector('#yarn'),label=document.querySelector('#yarnLabel'),bar=document.querySelector('#bar'),pct=document.querySelector('#pct'),loadText=document.querySelector('#loadText');let p={x:0,y:0},c={x:0,y:0,vx:0,vy:0},done=false,start=performance.now();const reduce=matchMedia('(prefers-reduced-motion:reduce)').matches;function clamp(v,a,b){return Math.max(a,Math.min(b,v))}function move(e){const r=stage.getBoundingClientRect();p.x=clamp(e.clientX-r.left,30,r.width-30);p.y=clamp(e.clientY-r.top,90,r.height-80);yarn.style.left=p.x+'px';yarn.style.top=p.y+'px';label.style.left=clamp(p.x+55,10,r.width-160)+'px';label.style.top=clamp(p.y-25,90,r.height-100)+'px'}stage.addEventListener('pointermove',move,{passive:true});function finish(){if(done)return;done=true;bar.value=100;pct.textContent='100%';loadText.textContent='READY.';setTimeout(()=>{intro.classList.add('hidden');final.classList.remove('hidden')},reduce?150:800)}function loop(t){if(done)return;const r=stage.getBoundingClientRect(),dx=p.x-c.x,dy=p.y-c.y,d=Math.hypot(dx,dy)||1;c.vx+=dx/d*.045;c.vy+=dy/d*.035;c.vy+=.018;c.vx*=.985;c.vy*=.985;c.vx=clamp(c.vx,-5,5);c.vy=clamp(c.vy,-7,7);c.x+=c.vx;c.y+=c.vy;const ground=r.height*.70;if(c.y>ground){c.y=ground;c.vy=-3.8}c.x=clamp(c.x,60,r.width-60);cat.style.left=c.x+'px';cat.style.top=c.y+'px';cat.style.transform=`translate(-50%,-70%) scaleX(${c.vx<-.1?-1:1})`;if(Math.hypot(c.x-r.width*.5,c.y-ground)<65&&bar.value>92)finish();requestAnimationFrame(loop)}function loading(t){if(done)return;const v=clamp((t-start)/(reduce?1600:6500)*100,0,100);bar.value=v;pct.textContent=Math.round(v)+'%';if(v<100)requestAnimationFrame(loading);else loadText.textContent='GUIDE THE CAT.'}requestAnimationFrame(()=>{const r=stage.getBoundingClientRect();p={x:r.width*.68,y:r.height*.60};c={x:r.width*.35,y:r.height*.60,vx:0,vy:0};move({clientX:p.x,clientY:p.y});requestAnimationFrame(loop);requestAnimationFrame(loading)});if(reduce)setTimeout(finish,1900);
const form=document.querySelector('#form'),email=document.querySelector('#email'),status=document.querySelector('#status');form.addEventListener('submit',async e=>{e.preventDefault();const value=email.value.trim().toLowerCase();if(!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(value)){status.textContent='ENTER A VALID EMAIL.';return}const button=form.querySelector('button');button.disabled=true;status.textContent='CONNECTING...';try{const r=await fetch('/api/waitlist',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email:value,website:form.website.value})});const d=await r.json().catch(()=>({}));if(r.ok&&d.success){status.textContent=d.alreadySubscribed?'YOU\'RE ALREADY ON THE LIST.':'YOU\'RE IN. SEE YOU ON THE OTHER SIDE.';form.reset()}else status.textContent=d.message||'SOMETHING WENT WRONG. TRY AGAIN.'}catch{status.textContent='NETWORK ERROR. TRY AGAIN.'}finally{button.disabled=false}})})();
